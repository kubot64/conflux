
# Dolt database files (added by bd init)
.dolt/
*.db

# beads issue tracker (local only)
.beads/

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key
*.keystore
*.p12
*.pfx

# Confluence CLI local state (contains history.json with page titles, alias.json)
.confluence-cli/

# Build artifacts
/conflux
